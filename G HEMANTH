 #include <stdio.h>
 int main() {
    float celsius, fahrenheit;
    printf("Enter Celsius: ");
    scanf("%f", &celsius);
    fahrenheit = (1.8 * celsius) + 32;
    printf("Fahrenheit: %.2f\n", fahrenheit);
    return 0;
 }
